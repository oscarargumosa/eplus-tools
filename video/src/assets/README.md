# Assets

Place logo files here:

- `logo-dark.png` — Dark version (blue text, for light backgrounds)
- `logo-light.png` — Light version (white text, for dark backgrounds)

Both should be PNG with transparent background.
